namespace MfiManager.Middleware.Enums {
    /// <summary>
    /// Insurance claim receipt status
    /// </summary>
    public enum ReceiptStatus {
        Entered = 0,
        Approved = 1,
        Rejected = 2
    }
}
