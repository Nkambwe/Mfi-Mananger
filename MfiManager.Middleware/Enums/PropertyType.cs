namespace MfiManager.Middleware.Enums {
    /// <summary>
    /// Enum types for data types
    /// </summary>
    public enum PropertyType {
        Null = 0,
        Int32 = 1,
        String = 2,
        Double = 3,
        DateTime = 4,
        Decimal = 5,
        Float = 6,
        Boolean = 7,
        Single = 8,
        TimeSpan = 9
    }
}
