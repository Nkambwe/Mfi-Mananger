namespace MfiManager.Middleware.Enums {
    /// <summary>
    /// Claim severity score
    /// </summary>
    public enum SeverityScore {
        Low = 1,
        Minimal = 2,
        High =3
    }
}
